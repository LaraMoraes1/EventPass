<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.EventPass"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.TeamRegisterActivity" />
        <activity android:name=".ui.ScanResultActivity" />
        <activity android:name=".ui.AdminDashboardActivity" />
        <activity android:name=".ui.ProfileActivity" />
        <activity android:name=".ui.HistoryActivity" />
        <activity android:name=".ui.ScannerActivity" />
        <activity android:name=".ui.QrCodeActivity" />
        <activity android:name=".ui.EventDetailActivity" />
        <activity android:name=".ui.EventFormActivity" />
        <activity android:name=".ui.EventListActivity" />
        <activity android:name=".ui.DashboardActivity" />
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
